using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class teamRowingController : MonoBehaviour {

    //One red column and one blue column
    //The screen will indicate which column for everyone to be standing in
    //Each synch increases the speed/score for the team

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
